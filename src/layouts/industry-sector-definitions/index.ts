import Page from './page';
const fetchData = async () => {};
const activeCustomToggles = () => {};

const pageContent = {
  entities: [
    {
      Title: 'SubTitle',
      renderString: (): string => `Industry sector definitions`,
    },
  ],
  filterToggles: [],
};

export { fetchData, activeCustomToggles, Page, pageContent };
